apiVersion: v1
kind: Service
metadata:
  name: {{ include "arbetern.fullname" . }}
  labels:
    {{- include "arbetern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arbetern.selectorLabels" . | nindent 4 }}
