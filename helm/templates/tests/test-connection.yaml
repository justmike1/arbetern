apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ovad.fullname" . }}-test-connection"
  labels:
    {{- include "ovad.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ovad.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
