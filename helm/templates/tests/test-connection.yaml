apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arbetern.fullname" . }}-test-connection"
  labels:
    {{- include "arbetern.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arbetern.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
